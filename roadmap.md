# Roadmap — workspace-manager

> Gestor del área de trabajo: contro **308A-7V13C — cierre de los 3 gaps de correctness de la auditoría (2026-09-01, §J-11V13C):** sentinel pinned 0.7.7 re-ejecutado en los 3 repos de V12 — gloryapi 0/0/0/0 exacto, coolify 81 (1e/57w/23h) y Laminal 5 (0e/5w) con deltas +15w/+5w TODOS css-hardcoded-value por reactivación de la regla en 0.7.6 (4a4a0f9, 318A-4; repos sin cambios, estable en 2 corridas → no regresión); glory-harness root-causeado: 3 warnings reales en core/src/llm.rs (1028 líneas: limite-lineas+nivel-2+funcion-larga-rs) a HEAD ff65ccfe en repo sucio WIP del usuario y omitido del scan POST (no se registra como excepción — hallazgo real fuera del set trackeado, documentado); mapper commiteado `scripts/quality/agregado-8787.mjs` que consume ambos shapes del 8787 (GET map por clave = cache, POST array = scan forzado), normaliza por proyecto y reporta el total — sin sweeps shape-frágiles. Verificación final sin ediciones de producción: harness 9/9 MANTENIMIENTO, sweep forzado total 1727 (PT fluctúa ±2 por WIP), type-check exit 0. Push pendiente del usuario. **308A-7V13 — harness + registro de excepciones + convergencia (2026-09-01, §J-11V13):** mejora estructural de la auditoría: `scripts/quality/analyze-blocks.mjs` (baseline CLI fijado → desglose → clasificación → veredicto MANTENIMIENTO/ACCIONABLE, con `--json`/`--detalle`, matcher de rutas normalizado a `/` para Windows) + `scripts/quality/excepciones.json` sembrado con las familias V2–V12 por proyecto (9 proyectos, 38 familias, evidencia §/commit). Criterio de convergencia: MANTENIMIENTO = 0 hallazgos fuera del registro; cualquier hallazgo nuevo revierte a ACCIONABLE. El harness destapó **7 claseHuerfana reales muertas** de workspace-manager que §I-15 daba por FP (`ejRutaLabel`, `ejDefault` ×4, `scanCfgCabecera`, `scanCfgTitulo` en paneles.css, 0 uso repo-wide) — borradas + 2 literales → varsense workspace-manager 93→78, 78/78 mantenimiento; registrado `configBadge--sin` (mismatch code/CSS preexistente) y `mapaV2Etiqueta` (compuesto usado). Verificado: 9/9 proyectos MANTENIMIENTO con 0 descubiertos (smoke coolify = 30 exacto 12/11/6/1, PT 731 = 1e/730w cubierto como WIP documentado), node --check y pnpm type-check exit 0, resto del área sin tocar. Push pendiente del usuario. **308A-7V12 — coolify/gloryapi/Laminal verificados como excepción documentada (2026-09-01, §J-11V12):** frentes declarados por el cierre V11. Baselines varsense con runtime 1a0c588: coolify 30 (0e) = claseHuerfana 12 (todos FPs dinámicos verificados línea a línea: vpsStatusBar${index+1}, ternarios VistaSitios/VistaPortalVisual, variantes Button.tsx), valorHardcoded 11 (one-offs portal VPS sin token), token-duplicate 6 (cross-dominio monocromo — colapsar acoplaría texto/fondo/acento/borde), cssInlineReact 1 (runtime menú); gloryapi 76 (0e) = 100% ya documentado §I-10 (puente @theme Tailwind v4 + aliasing shadcn + runtime dnd-kit); Laminal 42 (0e) = 100% ya documentado §I-11 (API pública tema Blender + runtime editor + one-offs gizmo/canvas WebGL2). CERO reales con token exacto — frente de verificación y documentación, sin forzar colapsos que acoplan dominios (lección V9/V10 aplicada). Verificado: 0 errores varsense, sentinel sin regresión, WIP intacto. Sin cambios en los 3 repos; solo docs workspace-manager (commit de este bloque). Push pendiente del usuario. **308A-7V11 — WANDORIUS 4 colapsos same-scope (2026-09-01, §J-11V11):** frente declarado por el cierre V10. Baseline varsense 157 (0e) = token-duplicate 58, claseHuerfana 10, cssInlineScript 89. Clasificación completa: los 58 duplicate son mayormente pares cross-scope de inversión de tema (knobs documentados [297A-18]/[308A-6], colapsar rompería dark mode), los 10 claseHuerfana son FPs dinámicos verificados (tag-estado--${status}, media-library__badge--${asset_state}, Tiptap DOM) y los 89 cssInlineScript son runtime del desktop (excepción documentada). **4 colapsos reales same-scope**: --win-w/--win-h → var(--sistema-ventana-*-default), --icono-row → var(--icono-col), --sistema-menu-contexto-texto-tamano → var(--tamano-pequeno), todos en :root con canónico definido una sola vez y consumidores verificados. Resultado: varsense 157→153, 0 errores, 0 hallazgos nuevos. Verificado: type-check exit 0, sentinel 0e/0w/0h. Commit WANDORIUS **4ef39ee1**; push pendiente del usuario. **308A-7V10 — RESTAURANTE excepción total + ONG AGAPE 10 clases muertas (2026-09-01, §J-11V10):** frente declarado por el cierre V9 (RESTAURANTE 141 / AGAPE 129 varsense con runtime 1a0c588). RESTAURANTE: clasificado completo — valorHardcoded 15/15 one-off de canvas (radios 2-6px, fonts 0.65-0.95rem de PlanoSala/PlanoOcupacion, sin token exacto), claseHuerfana 10/10 con uso = FPs, resto en submódulo glory-rs → excepción §I-15/§I-2, 0 cambios. AGAPE: de 26 candidatas huerfanas, 16 FPs dinámicos verificados (tarjetaAgape--${tono}, toast--${tipo}, panelEstadoChip--${status}, panelHistoriaCuadro--color${i}, cuadroAcerca--color${i}, botonEnlace--${variante}) y **10 muertas reales eliminadas** (marcaSimbolo/marcaTexto/marcaHoja, panelAviso/panelError/panelAccionesTabla, opcionalDonar, tituloBlog/enlaceInstagram, etiquetaHero) + tokens --textoMarcaSimbolo/--textoMarcaMini sin referencias retirados (--textoMarca conservado, PiePagina.css:38). Resultado: varsense 129→116, 0 errores, 0 hallazgos nuevos (token-duplicate 4/unused 4 sin cambios). Verificado: check:front + tsc exit 0, vite build verde, sentinel 0.7.7 149 = 0e/148w/1h con 0 hallazgos en CSS tocados (discrepancia vs 110 = runtime §J-9). Commit AGAPE **0532a99** (stage explícito); RESTAURANTE/PT sin tocar; push pendiente del usuario. **308A-7V9 — extensión metodología al área, valorHardcoded de workspace-manager (2026-09-01, §J-11V9):** bloque declarado por el cierre V8. Baselines área con runtime 1a0c588: RESTAURANTE 141, ONG AGAPE 129, workspace-manager 93 — el resto del área queda en excepciones ya documentadas. Único accionable: valorHardcoded ×42 de workspace-manager (frente J-3 enmarcado) → 6 reales con token exacto (fallbacks redundantes con token declarado en variables-v2.css:21-24/48-50) colapsados a var() en MenuContextual.css/paneles.css/v2.css (estos dos últimos también dejan `--ancho-*`/`--alto-consola` como única fuente, resolviendo los runtime del bloque anterior). Resultado: varsense 93→87, 42→36, visual-neutral (fallback inerte), 36 restantes one-off/tooltip sin token = excepción §I-15. Verificado: type-check exit 0, sentinel 31 (0e/27w/4h) = baseline exacto sin hallazgos nuevos (3 de paneles.css preexistentes), 0 errores. Commit workspace-manager f066fb5; PT y resto sin tocar; push pendiente del usuario. **308A-7V8 — mejora core varsense token-duplicate same-file (2026-09-01, §J-11V8):** bloque declarado por el cierre V7. Semántica: duplicado real = misma variable/valor idéntico dos veces DENTRO del mismo archivo; coincidencia cross-file/cross-dominio = match espurio. Fix en core (agrupar por (archivo, valor), canonical same-file) + 3 tests nuevos → 74/74, lint/check:core verdes. PT: duplicate 186→181 (−5 espurios cross-file: ptr-translateY/radioMinimo, space-xs/scrollbarAncho, arbol-color/iconoActivo, panelHeaderBorde/iconoBorde, space-sm/espacioTactil), audit FN = 0, varsense 736→731 (1e/730w, error = WIP usuario), 0 hallazgos nuevos. Release 1a0c588: pins+locks 10 consumidores (PT 75d302c, WANDORIUS 1768042c+31856ef1, workspace-manager 6ab9fe6, RESTAURANTE f317bcb, AGAPE 281f25b, GLORYPORT cf6a60a, Laminal 1fda566, coolify 0b70356); WANDORIUS submodule dist idéntico byte-a-byte + smoke contratos. Verificado: type-check exit 0, build verde, sentinel PT 95 (0e/87w/8h) = baseline, re-análisis forzado 8787 → varsense PT 1e/730w (runtime corre el fix). PT ahead 11; push pendiente del usuario. 308A-7V7 — token-unused/token-duplicate de PT cerrado (2026-09-01, §J-11V7):** bloque declarado como siguiente accionable (token-unused 19 + token-duplicate 186 de PT, varsense 736 = 1e/735w, runtime fijado `beb05ba`). Premisa del encargo («ceguera del detector por puente Tailwind v4») **falsada con evidencia**: PT no tiene Tailwind (0 `@theme`, 0 tailwind.config, 0 postcss.config) — no procede fix de core (a ciegas crearía FNs). token-unused 19: detector CORRECTO — 0 referencias `var()` repo-wide en el scope del scan, todos en variables.css (WIP del usuario) → remoción gated al usuario, excepción documentada. token-duplicate 186: hallazgos veraces (agrupación cross-file por valor) pero colapso no actionable — 177 en WIP del usuario y 9 fuera: coincidencias cross-dominio (`--space-xs` vs `--dashboard-scrollbarAncho`…) y pares paralelos runtime verificados a mano (`--ptr-translateY`/`--ptr-contenido-translateY`, hooks JS separados; `--col1-fr`/`--col2-fr`) cuyo colapso rompe comportamiento → patrón §I-7/§I-9/§I-10 documentado. CERO código tocado, sin disables nuevos. Verificado: type-check exit 0 + vite build verde + sentinel PT 95 (0e/87w/8h) = baseline exacto + varsense 736 estable. Re-análisis forzado 8787: PT 801 = 1e/792w/8h con duplicate 186 / unused 19 / claseHuerfana 429 — composición idéntica al CLI (desync sentinel runtime 0.7.4 = J-9 documentado). Sin commit en PT (nada que tocar); PT sigue ahead 9 de origin; push pendiente de decisión del usuario.l de proyectos, estado Git/Sentinel/VarSense/roadmap,
> mapa isométrico, gestión de AGENTS.md y skills. Rama primaria: `main`.
>
> **PLAN-CERO-DEUDA (2026-09-02) — campaña a cero, decisión del usuario («la meta es llegar a cero»):** nuevo plan `PLAN-cero-deuda-2026-09-02.md` (fuente única del frente de deuda real, sustituye el frente de 308A-5). Inventario 1.001 (0 errores) clasificado por tratamiento: **F1 ✅ CERRADO (308A-7V21)** — re-clasificación post-V20 de familias dinámicas (AGAPE ch 18→0 con 3 fixes de core: submódulos `.git` directorio + `imageClass:` + artefacto url(); WANDORIUS ch 6→3 con RC-4 `.push()`; REST 6 re-verificados vivos; WM: mapaV2Etiqueta confirmada MUERTA → F2; tests 92/92; harness 9/9 MANTENIMIENTO 0 drift: PT 438, AGAPE 78, WANDORIUS 146, resto invariante), **V22 ✅ CERRADO (308A-7V22, 2026-09-02)** — CORREGIR DETECTOR antes de F2 (decisión del usuario «primero corregir detector» tras la auditoría F2-PT: 123 muertas reales + 13 VIVAS/FP + 25-27 zona gris, no las ~154 que el plan esperaba). Core varsense rama `fix/318A-7V2`: M1 mapas Record<Tipo,string> por indirección (recordatoriosTexto×2), M2 templates que interpolan variable local (itemNotificacion--leida), M3 returns de IIFE flecha auto-invocada (barraRelleno×4), M4 clases en templates HTML runtime (scratchpadVistaPrevia×4), M5 fix removeComments con regex literales (bug preexistente que corrompía el estado de strings y ocultaba los `html +=` reales; whitelist prevSig + neutralización de comillas en salida). 53/53 tests, lint 0, check:core OK, dist rebuild. PT 163→**151** (12 resueltas: 11 VIVAS + scratchpadTextoTitulo destapada), 0 NUEVAS (0 FN). Harness 9/9 MANTENIMIENTO sin drift. Commit local varsense `21d8a70` + registro/plan WM `499d988` (sin push), F2 borrado de CSS muerta real (~123 PT tras V22 + mapaV2Etiqueta WM), F3 re-auditoría valorHardcoded con token exacto (AGAPE 69, coolify 11, WM 34, REST 15, Laminal 4, PT 14), F4 seams reales de runtime CSS, F5 tokens del WIP de PT (gated al commit del usuario), FASE FINAL decisión uno-a-uno de zona gris/unused (zona gris PT ~4, límite scanner ~24, puente @theme REST 88/gloryapi 75/AGAPE 8, cross-scope tema WANDORIUS 54/coolify 6/WM 4/PT 5, one-offs ~150-200, runtime sin seam ~180, monolitos). DoD: 0 pendientes sin decisión + harness 9/9 con registro decidido. Sin push (decisión del usuario).
>
> **308A-7V20 (2026-09-02, §J-11V20) — productores de clase fuera de atributo (core varsense):** mandato del usuario (FPs se arreglan en el detector). De los 199 claseHuerfana de PT, ~43 tenían uso vivo no indexado. 3 causas raíz probadas en `classIndexBuilder.ts`: (1) `clase += 'x'` nunca indexado (solo se leían declaraciones `=`); (2) transportadoras `clase*` consumidas cross-file no emitían tokens (usePullToRefresh→PullToRefresh); (3) funciones mapeadoras invocadas en atributos de clase no se resolvían a sus `return` literales (ListaProyectos etiquetas, IndicadorPlan). `cuerpoDeFuncionEn` tolera anotaciones TS. Tests 4 nuevos → **88/88**; dist reconstruido. Auditoría FN old→new: 35 desaparecidos verificados línea a línea (0 FN), 0 aparecidos. **Bug del harness corregido:** el drift solo marcaba familias con ≥1 hallazgo medido — caída a 0 silenciosa; ahora conteo registrado>0 que mide 0 = DRIFT. Destapó 3 zombies: coolify ch 5→0 (dinámicas legítimas V20), WM familia fp-dinamico stale (3→0 desde V18), PT `menu-contextual` = regla de **sentinel** fuera del scope varsense del harness (verificado 0e en sentinel 0.7.7). Resultado: PT 474→**439** (ch 199→**164**), coolify 23→**18** (ch 5→0), WM ch 3→2; 9/9 MANTENIMIENTO, 0 descubiertos, 0 drift, 0 errores. Registro sincronizado + nota corregida (544e3c8 = V18 publicado en V19; V20 en worktree local sin publicar). Restantes honestos PT: ~154 muertas reales (decisión usuario), ~24 límite scanner, ~4 zona gris. Commits locales sin push: varsense (318A-7V20) + WM (harness+registro+docs). **308A-7V19 (2026-09-02, §J-11V19) — alineación sentinel/varsense del área + reporter de desalineación:** pedido del usuario («alinea todo…» + «planifica que WM pueda reportar desalineación»). Nuevo `scripts/quality/verificar-alineacion.mjs`: por repo×herramienta compara pin declarado (quality-tools.json/sentinel.lock.json/gitlink) vs runtime (HEAD checkout compartido/submódulo) vs publicado (refs del origin: main/tags), estados ALINEADO/PIN_ATRASADO/SIN_PUBLICAR/PIN_ROTO, exit≠0 si hay desalineación. Corrida inicial ROJO (sentinel 9/9 ALINEADO 0559576; varsense 8/8 DESALINEADO: pins V14 38889aa vs runtime V18 544e3c8, publicados solo hasta J-8). A1: varsense publicado tag `v2.2.1-v18` en origin del checkout compartido. A2: 8 consumidores re-pineados a 544e3c8 con lock regenerado (WANDORIUS gitlink+dist submódulo). A3: **17/17 ALINEADO** — el reporter destapó que WANDORIUS varsense estaba NO-PUBLICADO (su .gitmodules declara github.com/1ndoryu/varsense.git y V14→V18 jamás se habían publicado a GitHub, preexistente desde V14); publicada la cadena completa con autorización del usuario: GitHub main `303e7f9..544e3c8` + tag `v2.2.1-v18` (fast-forward verificado), ahora los 8 consumidores son durables (los 7 sourcePath también resuelven contra GitHub en clone fresco). Commits locales sin push: PT 0690c23 · WM 9bd220d · REST dea8892 · AGAPE f4fea72 · coolify 6789390 · Laminal 2bf7abe · GLORYPORT ecc9896 · WANDORIUS d6b2d374+cdd44ee6. **308A-7V18 (2026-09-02, §J-11V18):** contexto `className` para familias dinámicas (pedido: FPs se arreglan en el detector). Root-cause: el «bug del índice de literales» era mayormente espejismo (removeComments correcto; compuestos reportan el otro miembro); el bug real era `REGEX_CLASS_TEMPLATE` sin soporte de templates anidados ni `.trim()`. Fixes: escáner balanceado de templates + `descomponerTemplate` anidado + guarda anti-prosa de V17 aplicada SOLO fuera de atributos `className`/`claseAdicional`. Tests 84/84, lint+check:core verdes. Auditoría FN old→new: 25 desaparecidos 0 FN (verificados línea a línea), 0 aparecidas. Resultado: PT 499→**474** (ch 224→**199**), AGAPE 103→**96** (ch 18), coolify 26→**23** (ch 5), WM 47→44; harness 9/9 MANTENIMIENTO, 0 descubiertos, 0 drift. Registro sincronizado. Restantes honestos PT: ~154 muertas reales (borrado = decisión del usuario), ~24 límite scanner, ~4 zona gris retenidas. **308A-7V17 (2026-09-02, §J-11V17):** fixes de detección en core de varsense (pedido del usuario: submódulos no reportan + FPs se arreglan en el detector, no a mano). 3 causas raíz: (1) walker descendía a submódulos (glory-rs/tools, `.git` archivo) → excluidos por diseño; (2) `REGEX_VAR_CLASS_DECLARATION` no-greedy tragaba `const clases=[...]` de componentes (Boton.tsx) → escáner balanceado registra familias; (3) prefijos de familia extraídos de prosa (`titulo={`...`}`) absorbían huérfanas reales → prefijo = token pegado a `${` con forma de clase (multi-clase `badgeInfo badgeInfo--${v}` preservado). Tests 82/82, lint+check:core verdes. Auditoría FN old→new: 46 desaparecidos 0 FN (dinámicos verificados + glory-rs), 9 aparecidos = muertas reales re-reportadas. Resultado: PT varsense 536→**499** (claseHuerfana 261→**224**), RESTAURANTE 141→**137** (ch 10→6), resto invariante; harness 9/9 MANTENIMIENTO, 0 descubiertos, 0 drift. Registro `excepciones.json` sincronizado (conteos + evidencia V17). Decisions del usuario pendientes: push y re-alineación del runtime 8787. **308A-7V16 (2026-09-02, §J-11V16):** registro verificable por máquina + harness regla-por-regla. `excepciones.json` migrado a formato v2 (pares token-duplicate tipados con tokenA/tokenB/archivo/categoria/lineas/evidencia + familias con conteo por regla/archivo); `analyze-blocks.mjs` casa cada hallazgo contra el registro (pares concretos + drift de conteos) y solo declara MANTENIMIENTO con 0 descubiertos/0 drift. Corregida la verificación falsa de V15: PT salía «536/536 MANTENIMIENTO» sin cubrir los 175 pares de `variables.css` (WIP del usuario) — registrados como familia WIP, PT vuelve a MANTENIMIENTO con cobertura real. Docs deduplicadas (plan/completados/roadmap enlazan a la fuente única; tablas duplicadas eliminadas). Verificado: 9/9 MANTENIMIENTO, 0 descubiertos, 0 drift, JSON parsea, node --check OK, ningún repo de producción tocado. Commits locales sin push (WM + PT docs).
>
> Fuentes canónicas: `PLAN.md`, `AGENTS.md`, planes en `PLAN-*.md` y `roadmap.md` de cada proyecto.

## Siguiente bloque ejecutable

**308A-5 — Corregir hallazgos restantes de Sentinel y VarSense (piso honesto + wins rápidos).** Decision del usuario (2026-08-31): alcance = piso honesto + wins rapidos (refactors verdes + excepciones legitimas documentadas, sin disables para bajar conteo; sin forzar monolitos de API/UI de gran superficie). Plan en `PLAN-corregir-restantes-sentinel-varsense.md`. **FASE A COMPLETA (2026-08-31):** A1 destrabado — causa raiz del estado `error` de coolify-manager-rs/ONG AGAPE = `correrSentinel` descartaba el stdout cuando `sentinel analyze` sale con exit != 0 (lo dispara cualquier hallazgo de severidad 'error'); fix en `analizador.ts` (parsear `err.stdout` en el catch) verificado con type-check + smoke real: **coolify-manager-rs 124 (1e/100w/23h)** y **ONG AGAPE 369 (26e/278w/11i/54h)** ya salen `conHallazgos`. A2 varsense gloryapi creado (`varsense.config.json`, preset node; doctor exit 0, analyze 0/0/0/0 sin regresión); no se amplio quality-tools/lock por ser consumidor legacy (`task:check`) — provisión completa de varsense pendiente de la migracion del gate legacy. **FASE B1 COMPLETA (2026-08-31):** freebuff-bridge 20→0 (`sentinel analyze` 0/0/0/0): logger central `src/logger.ts` + `loggerModules` en sentinel.config; barrel `cargarConfig`→`src/config.ts` (index puro); ISP `ResultadoTarea` split por intersección (`ResultadoTarea` 8 + `ResultadoTareaMeta` 4, sama shape runtime). Tsc exit 0, cli salida intacta, tests 30/31 (1 cuelga preexistente H2/H3 SSE-abort ~40s al correr archivo completo, pasa aislado — pendiente test-infra, ajeno a B1). **FASE C PARCIAL (2026-08-31):** workspace-manager 83→**76** (`tsc --noEmit` exit 0, `sentinel analyze` exit 0 / error 0, sin regresión; evidencia por regla en §C del plan). C1: se INTRODUJO el componente `Button.tsx`/`Button.css` canónico (monocromo strict, tokens variables-v2, variantes cuadrado/textoLg) y se refactorizaron MapaV2(3)+NavBar(2) → `css-adhoc-button-style` 5→2 (los 2 restantes en `paneles.css` ajeno). C2: `inline-style-prohibido` 6→3 — los 3 estáticos a `app.css`/`isoMap.css`; los 3 dinámicos (tooltips/cursor relativos al viewport) documentados como inline legítimo. C4: `limite-lineas` 6→5 — el único split no arbitrario fue `etiquetas.ts` 360→294 (bloque de reglas extraído a `etiquetas-reglas.ts`/`CATALOGO_REGLAS`, merge por spread); el resto son monolitos de gran superficie (useWorkspace/server.index/EditorEsquema/PanelConfig/paneles.css) documentados. C3: window/dom restantes son boundary legítimo del shell (clamping tooltip, dismiss de menú, mount React) y los de `etiquetas.ts` falsos positivos (strings del catálogo); documentado, sin abstracción sin segundo consumidor. **FASE B2 COMPLETA (2026-08-31):** GLORYINSPECTOR 2→0 — `directoryExceptions = ["inspector","tests"]` (namespace plano de paquete Python cohesivo; reorganizar partiría la API pública; ya hay subdirs `adapters/`/`core/`). `sentinel analyze` 0/0/0/0, `pytest` 51 passed. **FASE B3 PARCIAL (2026-08-31):** workspace-manager 86→**83** (`tsc --noEmit` exit 0, analyze exit 0 / error 0, sin regresión). Resuelto el `key-index-lista` (2→0: `EditorJson` key=label, `EditorEsquema` key=ruta). Excepciones documentadas del lote: `css-adhoc-button-style` (5 — el proyecto no tiene componente `Button`, forzarlo es Fase C; 2 en `paneles.css` ajeno) y `promise-sin-catch` (1 — falso positivo, las 3 cadenas ya tienen `.catch`); `import-muerto`/`todo-pendiente` ya en 0. La Fase B queda así cerrada (sin forzar CSS/console que son Fase C/D). La **Fase C (piso workspace-manager) está en el piso honesto: 76** (Button canónico, inline estáticos movidos, split de etiquetas; el resto son excepciones documentadas de gran superficie/falsos positivos). **FASE D COMPLETA (2026-08-31):** RESTAURANTE 120→**114** (`sentinel analyze` exit 0 / error 0; `cargo check --tests` vía run-cargo.mjs exit 0, `tsc --noEmit` en frontend exit 0; evidencia por regla en §D-1 del plan). Corregidos: `console-production` 2→0 (logger central `frontend/src/utils/logger.ts` creado — la whitelist `loggerModules` ya lo declaraba pero el archivo no existía — y `ErrorBoundary`/`useNotificaciones` enrutan por él); `key-index-lista` 6→3 (chart.tsx ×2, DashboardReservas ×2); `todo-pendiente` 2→1 (TODO real de preflight extraído a constante con contrato; el restante en `bdp_sync.rs:675` es falso positivo — «order pendiente», término de negocio). Documentado, no forzado: `broadcast-mutex-riesgo-rs` 5 (patrón SSE idiomático y seguro verificado: BroadcastStream con filtro de Lag, capacidad acotada, send-error intencional; la regla solo acepta `sentinel-disable` — prohibido), `limite-lineas` 37+4/`inline-style` 18/`funcion-larga` 12/`parametros-excesivos` 11/`large-interface-isp` 10/`handler-accede-bd-rs` 8 (monolitos de gran superficie o refactor de capa repository), `key-index` 3 restantes (skeletons y listas estáticas read-only donde index es correcto), SQL crudos de `sqlx::query` como deuda sin forzar `query_as!`. Nota entorno: la DB local de dev tenía drift (faltaba la migración `haddock_venta_tracking` y `_sqlx_migrations` tiene checksum roto en `20260325000000`, por lo que `sqlx migrate run` no aplica); se aplicaron las 3 columnas aditivas (`IF NOT EXISTS`) directamente en `127.0.0.1/glory_db` para poder verificar — equivalente a `sqlx::migrate!()` del arranque. **FASE ONG AGAPE COMPLETA (2026-08-31):** 369→**164**, **errores 26→0**, `sentinel analyze` exit 0 (162w+2h); `cargo check --tests` (run-with-db.mjs) exit 0, `tsc --noEmit` en frontend-v2 exit 0; evidencia en §D-2 del plan. Los **26 errores y 173 hallazgos estaban en los submodulos fijados** (`glory-rs/`, `tools/sentinel/`, `tools/varsense/`) → `excludePatterns` += `**/glory-rs/**`/`**/tools/**` (scope canónico, código externo no propiedad del proyecto). Boundary legítimo declarado por archivo en `portableBoundaries` (window 18→0, dom 6→0: App/main.tsx, modales, vistas admin/donar/inicio — lista exacta, no rutas amplias). Logger central `frontend-v2/src/utils/logger.ts` + `loggerModules` (console-production 1→0, log conservado). Split real `funcion-larga-rs` (create_donation 108→32 líneas: parseo multipart extraído a `parsear_donacion`/`DatosDonacion`, misma validación). `directorio-abarrotado` 6→1 (`directoryExceptions` canónicos: src/handlers, src/repositories, components/ui, features/admin, features/inicio; raíz del repo documentada como estructura legítima). Documentado sin forzar: sqlx 55+6 (deuda, sin `query_as!` en esta pasada), limite-lineas 9 (monolitos de UI/admin/repos), css-elemento 25 + button-clase 13 + css-adhoc 4 + html-nativo 11 (el UI kit de frontend-v2 NO tiene Button/Select → migrar exige introducir design system, igual que Fase C de workspace-manager), usestate 11 + componente-sin-hook 12, emoji 8 (copy marketing), modal 3, key-index 2 (listas estáticas/posicionales donde el índice es la identidad), inline-style 1 (barra de progreso dinámica). Luego -> **E/F** PT 23 y GLORYPORT 1 excepciones -> **G** VarSense. **FASE G COMPLETA (2026-08-31) — VarSense auditado y expuesto:** verificada la provisión real (`<area>/.quality-tools/varsense` v2.2.1 funciona; el comentario «sin runtime» era stale de antes de 308A-1) y que `sentinel analyze` **NO** capturaba sus hallazgos (0 con source VarSense; corría solo en el gate-wrapper). Medición real: gloryapi 92 (37e/54w/1i — `variableNoDefinida` ×37 falsos positivos por `variableFiles: []` en su config, misconfig documentada), RESTAURANTE 172 (0e/109w/24i/39h — reales: token-duplicate/unused, valorHardcoded, claseHuerfana, cssInlineReact, propiedadProhibida → frente de tokens CSS propio, no forzado), GLORYPORT 0. **Implementado (DoD de G):** el manager ahora corre `varsense all` junto a `sentinel analyze` cuando el proyecto declara `varsense.config.json` y fusiona ambos reportes en el mismo `AnalisisSentinel` — hallazgos tagueados con `fuente: 'sentinel'|'varsense'` (`types.ts`), `correrVarsense()` con frescura que incluye versión de varsense + hash de su config (`analizador.ts`; antes un cambio de config de varsense no invalidaba la cache), proveedor de varsense reporta runtime real 2.2.1 (`proveedor.ts`, antes stale null/estatica), y UI: `PanelDetalle` muestra `sentinel: … · varsense v2.2.1: …` y `PanelConsola` etiqueta `[varsense]` los hallazgos de esa tool. Verificado: `pnpm run type-check` exit 0 + smoke real de `analizarProyecto` (gloryapi 92 solo-varsense, RESTAURANTE 114+172 fusionados sin tocar sentinel, GLORYPORT 0 sin cambiar total). workspace-manager queda en **78 hallazgos con cero nuevos de G** (los hallazgos en archivos tocados por G ya existían; el «76» de C era variación de medición — `analizador.ts` ya tenía 325 líneas pre-G). Deuda abierta: limpieza de tokens de RESTAURANTE (fase propia). **DEUDA gloryapi CERRADA (2026-08-31):** `variableFiles` corregido de `[]` a `["client/src/index.css"]` (único CSS con tokens `:root`; patrón RESTAURANTE/WANDORIUS) — `variableNoDefinida` 37→2 (los 2 restantes son runtime dnd-kit con fallback, sin mecanismo de exclusión en el schema; documentados), sin cambios en valorHardcoded 27/claseHuerfana 27/cssInlineReact 1 (reales). Al poblar el índice afloran `token-duplicate` 36 + `token-unused` 38 sobre index.css, clasificados como excepción legítima: aliasing semántico shadcn (valores base repetidos a propósito) y puente Tailwind v4 `@theme inline` (tokens consumidos vía utilidades compiladas, invisibles al escaneo estático). Conteo varsense 92→**131** (35 FP eliminados + 74 reales visibles), `sentinel analyze` gloryapi sigue 0/0/0/0, sin disables ni código tocado. Evidencia en §G-DEUDA del plan. Conteos autoritativos en la cache viva `/api/gate/analisis`; NO usar `_analisis.json` (volcado viejo). **FASE E/F COMPLETA (2026-08-31) — excepciones re-verificadas:** PROYECTO TASKS **23 exactos** (19w/4h/0e, exit 0) — coincidencia 1:1 con lo documentado (emoji-en-codigo 9, inline-style-prohibido 5, limite-lineas 4, funcion-larga-rs 1, parametros-excesivos-rs 4; monolitos de API store.ts/runtime.rs/agente.rs/ai.rs sin forzar). GLORYPORT **1 exacto** (`limite-lineas` popup.rs L1275 — monolito documentado, ahora 1275 líneas). Desviación aparente de GLORYPORT 1→2 (`directorio-abarrotado` en la raíz) era **artefacto de medición**: la raíz tiene exactamente 10 archivos propios (límite) y volcar el reporte DENTRO del repo contaba el archivo de salida como el #11; con salida fuera del árbol vuelve a 1. Lección registrada en el plan: medir E/F con el JSON fuera del proyecto. Árboles limpios (el `auth.rs` ajeno de PT se resolvió solo en otro hilo). **308A-5 COMPLETO (2026-08-31):** todas las fases A→G cerradas — A1 destrabe, A2 varsense gloryapi, B1 bridge 20→0, B2 GLORYINSPECTOR 2→0, B3 workspace-manager lote 86→83, C piso workspace-manager 83→78, D RESTAURANTE 120→114 + ONG AGAPE 369→164, G VarSense auditado y expuesto, E/F excepciones verificadas. Deudas abiertas documentadas (no bloqueantes): tokens CSS de RESTAURANTE (172 varsense) y monolitos de gran superficie en coolify-manager-rs/PT/GLORYPORT — la deuda de `variableFiles` de gloryapi quedó **CERRADA** en esta misma sesión (ver bloque FASE G arriba).
**308A-6 — Reducción del agregado 2571 en la consola (misconfig `variableFiles` + tractables seguros).** Origen del 2571: tras la fase G la consola agrega sentinel+varsense fusionados (cap 500/proyecto) y el grueso nuevo eran **727 errores `variableNoDefinida` de varsense** por la misma misconfig ya corregida en gloryapi: `variableFiles: []` en workspace-manager (382) y coolify-manager-rs (341) — índice de tokens vacío → todo `var(--…)` reportado no definido. **FIX APLICADO Y VERIFICADO (2026-08-31):** `variableFiles` corregido en ambos (workspace-manager → `["src/styles/variables.css", "src/v2/styles/variables-v2.css"]` — los `--v2-*` viven en `variables-v2.css`; coolify-manager-rs → `["gui/src/estilos/variables.css", "gui/src/estilos/portal.css"]` — los `--vps*` se definen bajo el scope `.vpsPortal`, el índice acepta scopes de clase; 4 paths verificados existentes). **Resultado varsense:** workspace-manager errores 382→**18** (16 en `paneles.css` ajeno + 2 runtime con fallback, documentados), coolify-manager-rs **341→0**. GLORYPORT/freebuff-bridge/GLORYINSPECTOR también tienen `[]` pero **no tienen CSS** (inofensivo, no se toca). **Tractables sentinel en workspace-manager (78→60, 0 errores, exit 0):** `console-production` 17→2 (logger central `src/shared/logger.ts` creado, whitelisteado por defecto por `DEFAULT_LOGGER_MODULES`; logs legítimos de catch enrutados en useWorkspace/server.index/AppV2; los 2 restantes en `MapaV2.tsx` ajeno), `fallo-sin-feedback` 4→2 (la regla solo cuenta `console.*` directo; los 2 restantes en MapaV2 ajeno), `promise-sin-catch` 1→0 (falso positivo real: `.catch` a 29 líneas del `.then`, ventana del analyzer 20 — refactor honesto en PanelConfig extrayendo el parseo), `acceso-api-sin-fallback`→0 (`data.entradas ?? []`). Gotcha resuelto: la extracción inicial de PanelConfig usó `editado` colisionando con un estado → falso positivo `mutacion-directa-estado`; renombrados los locales → cero hallazgos nuevos. Verificado `pnpm run type-check` exit 0 + `sentinel analyze` workspace-manager exit 0 (0e/56w/4h). Sin disables, sin commit, `paneles.css`/`MapaV2.tsx` ajenos intactos. Evidencia en §H del plan. **coolify-manager-rs (124 → 96, verificado):** `unwrap-produccion-rs` 5→0 (riesgo real, `?`/`ok_or_else` + variante `CoolifyError::Internal`), `directorio-abarrotado` 5→1 (`directoryExceptions` canónicas; el restante es la raíz con 16 archivos de manifests — excluirla por nombre matchearía toda ruta = disable repo-wide, excepción documentada), boundary window/dom→0 (`portableBoundaries` del shell GUI), splits reales de `limite-lineas` 10→6 por seam de dominio (theme_migrations, api/metrics, maintenance_render, sync_env_helpers con tests, infra/encoding, VistaPortalConsola/Visual). El error restante es `limite-lineas-nivel-3` de deploy_service.rs (2135 líneas, monolito documentado — split arbitrario, no se fuerza). Verificado `cargo check --tests` exit 0 + `tsc --noEmit` del GUI exit 0 + varsense 99/0 errores + cero hallazgos nuevos en archivos editados (solo familia de monolitos preexistente; portal.css sin diff). Gotcha resuelto: módulos helper deben declararse en el `mod.rs` padre (un `mod` dentro del archivo-hijo busca `archivo/helpers/` → 20 errores E0583/E0425 corregidos). Evidencia en §H-1 del plan. **CIERRE 308A-6 — agregado vivo verificado:** `curl /api/gate/analisis?analizar=todo` (servidor 8787 arriba) + reproducción del cálculo exacto de PanelConsola (problemas + hallazgos cap 500/proyecto + vulnerabilidades): **TOTAL 2571 → 1974 (−597)**. Cruce 1:1 con el bloque: workspace-manager 215 hallazgos = 60 sentinel + 155 varsense (18 errores = los documentados: 16 paneles.css ajeno + 2 runtime); coolify-manager-rs 195 = 96 + 99 (1 error = deploy_service documentado); gloryapi 2 = runtime `--sortable-*` (G-DEUDA). Errores visibles: **23, todos documentados** (los 2 de ONG AGAPE — `--colorRojoOscuro` con fallback correcto `var(…, var(--colorRojo))`, degradación válida, diff ajeno — se documentaron en H-2). Nota: el server usa su runtime global (fuente: runtime); para los 3 proyectos del bloque coincide exacto con las verificaciones manuales; PT/ONG AGAPE difieren de las verificaciones por binario fijado (preexistente, deuda de alineación de runtime ya registrada). Evidencia en §H-2 del plan. **CIERRE COMITS + planificación del resto (2026-08-31):** el usuario confirmó que soy el único agente y autorizó commits salvo PROYECTO TASKS. Commiteados los repos del bloque: workspace-manager `5863474`, coolify-manager-rs `d4f9f15`, gloryapi `e7157ce`, freebuff-bridge `42cf5b0`, ONG AGAPE `6f4cbb6`. Además corregí el error varsense pendiente de ONG AGAPE: **token `--colorRojoOscuro` añadido → 2 errores → 0** (0e/155w/1i/4h, tsc exit 0). §I del plan documenta la estrategia por familia del agregado con evidencia: (I-2) **`claseHuerfana` (616) es falso positivo del scanner** (no ve construcción dinámica de clases por ternarios/`${base}--${estado}`/variables indirectas → borrarlas rompería UI; corrección del analyzer, candidato upstream, no se tocan clases); (I-3) la discrepancia PT/WANDORIUS ya no es runtime — todos fijan el mismo commit y los extras de PT son varsense real que E/F no corría; (I-4) resto = deuda de arquitectura documentada (monolitos, tokens, CSS del GUI sin design system). Único no commiteado a propósito: **`freebuff/`** (diff grande no reconocido de mi trabajo; `bun` no disponible para verificar; posible WIP del usuario), explicado al usuario. **GUI de coolify-manager-rs — design system (308A-6GUI, hecho 2026-08-31):** frente tractable del design system del GUI. `css-elemento-html-directo` **25→0** (selectores de elemento `.{clase} button/h1-h3` movidos a clases con `className` en JSX, visual-neutral, títulos por sección colapsados) y `button-clase-especifica` **12→0** (clase `vpsBotonBase`→`vpsControlBase`, no choca con la whitelist). Login → componente canónico `Button` variant `primario` (`css-adhoc` ×1 y `.botonLogin` eliminados; `cursor`/`:disabled` al `.boton` en componentes.css). `inline-style-prohibido` de MenuContextual → CSS custom properties `--menuPosTop`/`--menuPosLeft` con clase modificadora (no rompe el reposo de SelectorPersonalizado); defaults en variables.css (VarSense indexa solo `variableFiles`). **Resultado: GUI 28→3** (2 warning preexist. portal.css + 1 hint ISP tipos.ts, documentados); sentinel proyecto **96→67** (1e/43w/23h; 1 error = monolito deploy_service documentado) sin regresión; varsense **0 errores**. Verificado `cargo check --tests` exit 0 + `tsc --noEmit` exit 0. Commit `308A-6GUI`. Evidencia en plan §I-6. Deudas restantes ya documentadas (tokens RESTAURANTE, monolitos). Restan deudas ya documentadas (tokens RESTAURANTE, monolitos). **RESTAURANTE — design system (frente varsense 172→140, hecho 2026-08-31):** borradas **20 `claseHuerfana` reales** (verificadas 0 usos repo-wide con word-boundary en `.ts/.tsx`, excluyendo fixtures y `glory-rs`) de `PlanoSala.css`/`PlanoOcupacion.css` (planoSala/planoBarraHerramientas/planoZonas, planoOcupacion/Titulo/Zonas/ZonaTab/activa/leyenda/indicador); conservados los `.mesaOcupacion.*` y formas/estados construidos dinámicamente con `${estado}`/`${mesa.forma}`. **Resultado varsense 172→140** (0e/77w/24i/39h): `claseHuerfana` 34→14, `valorHardcoded` 26→15, `propiedadProhibida` 7→6. Documentado sin forzar: 81 de `index.css` (token-duplicate 42 + token-unused 39) = aliasing semántico del puente shadcn/Tailwind v4 (no se colapsa), 10 claseHuerfana restantes = falsos positivos del scanner (§I-2), `cssInlineReact`/resto = posicionamiento dinámico legítimo. Verificado `tsc --noEmit` (22 errores, todos del submódulo `glory-rs` preexistentes, cero de `src/`), `cargo check --tests` exit 0, `sentinel analyze` sin hallazgos en archivos tocados. Evidencia en plan §I-7. **RESTAURANTE valorHardcoded — verificación (frente cerrado 2026-08-31):** los 15 restantes **no son colores de marca** — desglose exacto: 8 `font-size` + 7 `border-radius`, uno-off del lienzo del plano (mesas/handles/minimapa/indicadores). Todos los colores del plano ya consumen tokens `var(--…)`; los únicos literales son `oklch(0.7 0.2 142)` (verde "ocupada") que el detector no marca y `no_show` ya usa `--destructive` (asimetría candidata a token de estado, decisión de diseño no forzada). No hay escala que honrar sin cambiar diseño: `index.css` tiene 0 tokens de font-size y la escala de radio shadcn es rem (no coincide con los px del canvas) — excepción fundamentada, sin código tocado, `valorHardcoded` 15 = floor honesto sin regresión (0 errores, evidencias I-7). Siguiente frente enmarcado: WANDORIUS/agregado. Evidencia en plan §I-8. S0/S1 completadas; S2 avanzada: S2-01 a S2-04, S2-09 (categorías `sin commit` y `huérfanos`), S2-10/S2-11 (config de 36 a 0 problemas) y **S2-13 (consolidación de AGENTS.md completada)**: instrucciones de los 8 proyectos consolidadas en la sección §9 del `AGENTS.md` raíz; los `AGENTS.md` secundarios se eliminaron con `git rm` por repo tras backup S0 verificado (58/58 + `disk-v2` de WANDORIUS). **Frente de 1408 corregido (hilo 308A-2/1408):** RESTAURANTE llegó a su piso honesto **120** (`2fa1e652`, pusheado a `glory-rs-rest`), gloryapi cerró por completo **8→0** (`4e97e2a`, `tsc -b` + 315/315 tests) — F5 completo — y PROYECTO TASKS bajó de 500 a **23** (`ac5d4c4`: console-production 86→0 con logger central + `portableBoundaries.loggerModules` corregido). WANDORIUS COMPLETO 410→0 en `c1af8af6` (analizador 0.7.4 = 0 hallazgos en 481 archivos sobre rama `main` limpia; exclusiones canónicas, logger boundary, disables justificados de sqlx/handler-bd en fixtures, boundaries de plataforma, splits reales de limite-lineas; `main` sincronizada con `legacy-wandorius/main` 0/0). Los 23 restantes de PT son excepciones legítimas documentadas (emoji/copy dinámico, estilos inline dinámicos y monolitos de API: `store.ts`, `runtime.rs`/`agente.rs`/`ai.rs`, params/funcion-larga con firma pública) y el único matiz documental de WANDORIUS (primaria declarada `wandorius` vs rama activa real `main`). **WANDORIUS varsense — design system (frente 186→173, 0 errores, hecho 2026-08-31):** config `varsense.config.json` ya correcta (`variableFiles` = variables.css, sin misconfig). Borradas **12 `claseHuerfana` reales** (verificadas 0 usos ts/tsx repo-wide con word-boundary; el test `preferences-panel.test.ts` aserta la ausencia de `preferences-conflict` — [297A-13] conflicto auto-resuelto por LWW): `account-app__error/__secondary`, `font-panel` (pages.css), `preferences-conflict` base + `__message/__values/__actions` y `workspace-overlay-conflict` ×5 (Overlay.css); conservado `preferences-conflict__title` (3 usos reales). Conservadas como falsos positivos del scanner (§I-2): `tag-estado--archivado`, `media-library__badge--processing/--rejected` (construcción dinámica `${status}`/`${asset_state}`) + `tiptap`/`ProseMirror`. `token-duplicate` 59→58: el único gemelo literal same-scope (`--sistema-borde-doble`) → alias `var(--sistema-borde)`; los 58 restantes = aliasing intencional ya en `var()` (--fuente-*), paralelos `--sistema-*`/`--color-*` con overrides de scope distintos en dark mode (varios pares comparan definiciones del scope oscuro vs root) o igualdad coincidental entre knobs semánticos distintos (`--win-x`/`--menu-spacing`, `--espacio-md`/`--tamano-titulo`) — colapsarlos quitaría superficie de configuración del diseño B&W minimalista, excepción documentada. Verificado `npm --prefix frontend run type-check` exit 0 (cambios CSS-only, backend intacto), varsense fresco **173 = 0e/84w/89i** (−13 exactos sin regresión, `cssInlineScript` 89 = runtime del escritorio intacto), `sentinel analyze` **0/0/0/0** (baseline sin hallazgos nuevos). Evidencia en plan §I-9. **gloryapi varsense — design system (frente 131→77, 0 errores, hecho 2026-08-31):** config `varsense.config.json` ya correcta (`variableFiles` = client/src/index.css, sin misconfig). Borradas **27 clases `token-platform-*` muertas** de `index.css` (colores de marca por proveedor: google/groq/cerebras/ollama/…) — cada una verificada con 0 referencias en client/src y server/src (.ts/.tsx, grep literal + construcción dinámica `` `token-platform-${…}` `` — patrón de badge que nunca llegó a usarse). Las mismas 27 líneas eran el origen de 27 `valorHardcoded` (`#4285f4` etc.): el borrado resolvió ambas familias a la vez (−54 hallazgos exactos). Documentado sin forzar: los 74 restantes de `index.css` = puente shadcn/Tailwind v4 (`token-duplicate` 36 aliasing semántico + `token-unused` 38 pares shadcn), 2 errores runtime dnd-kit + 1 info cssInlineReact ya documentados. Verificado `tsc --noEmit -p client/tsconfig.app.json` exit 0 (cambios CSS-only), varsense fresco **77 = 2e/36w/1i/38h** (0 errores nuevo, sin regresión), `sentinel analyze` **0/0/0/0** (282 archivos). Evidencia en plan §I-10. **Glory-Laminal varsense — misconfig de alcance + design system (frente 124→91, 0 errores, hecho 2026-08-31):** `includePatterns` solo cubría `src/**` pero los CSS reales del proyecto viven en `styles/` (editor.css/widgets.css, 111 usos `var()` reales) — el snapshot de uso de tokens ignoraba los consumidores (patrón G-DEUDA). Verificado antes: todas las `var(--…)` de `styles/*.css` están definidas en `variables.css` (cero `variableNoDefinida` nuevos). Añadido `styles/**/*.css` a `includePatterns`; con el alcance corregido el baseline real era 124→115 (los 46 `token-unused` bajaron solos a 17 y afloraron 20 `valorHardcoded` que la misconfig ocultaba). **`valorHardcoded` 20→4** con tokens nuevos justificados por segundo consumidor real: 5× `#ffffff` sobre selección → `--textoSeleccion`, 4× `#4a4a4a` de hover/activo → `--interaccion`, 2× `2px` → `--radioFino`, y 5 más a tokens existentes (`--botonInternoMenu`/`--bordeSuave`/`--editorContorno`/`--textoChico`/`--radioArea`); 4 one-off de 1 uso documentados como excepción (`#2c2c2c`, `#5f5f5f`, `#334d80`, `rgba(…,0.12)`). **`token-duplicate` 6→1** (5 pares same-scope colapsados a alias `var()`: `--fondoHeader`/`--fondoPanel`→`--fondoStatusbar`, `--fondoInput`→`--fondoBase`, `--fondoInputSel`→`--fondoTopbar`, `--borde`→`--fondoPanelCabecera`, `--seleccion`→`--fondoHoverMenu`; el 1 restante es auto-colisión de los dos alias al mismo token — el detector no resuelve `var()`, límite del analizador). `token-unused` 46→14 (API pública del tema Blender sin consumir aún, excepción). `claseHuerfana` 49 intactas = falsos positivos §I-2 verificados (uso real vía `createElement(tag, className)` posicional + `classList.toggle`, inadmistibles para el scanner). Verificado `npm run type-check` exit 0 (CSS-only), varsense fresco 91 = 0e/54w/23i/14h (−33 netos reales, sin regresión), `sentinel analyze` 0/0/0/0 intacto. Commit `308A-6GL`. Evidencia en plan §I-11. **CIERRE DEL CICLO — agregado vivo re-verificado (TOTAL 1797, 2026-08-31):** `curl /api/gate/analisis?analizar=todo` con el servidor 8787 arriba + cálculo exacto de PanelConsola (suma de hallazgos cap 500/proyecto; la respuesta actual no incluye problemas/vuln). La cache de Glory-Laminal estaba stale (19:14, previo al frente) → re-análisis forzado puntual `POST /api/gate/analizar` `{clave: Glory-Laminal, forzar: true}` → **91** coincide 1:1 con la verificación manual de §I-11. **TOTAL consola: 1974 → 1797 (−177); desde el 2571 inicial: −774.** Desglose: PT 500 (cap; runtime real 1718w preexistente), ONG AGAPE 324 (2e→0 por §I-1), RESTAURANTE 250 (286→250, §I-7/8 reflejado), workspace-manager 215 (18e documentados, sin cambios en este frente), WANDORIUS 173 (§I-9), coolify-manager-rs 166 (195→166: GUI §I-6 + varsense), Glory-Laminal 91 (§I-11, re-análisis 21:13), gloryapi 77 (§I-10), GLORYPORT 1, freebuff-bridge/GLORYINSPECTOR 0. **Errores visibles: 21, todos documentados** (18 ws-manager: 16 paneles.css ajeno + 2 runtime; 1 coolify deploy_service monolito; 2 gloryapi runtime dnd-kit). Sin hallazgo inesperado → sin commit en el cierre, solo plan §I-12 + este log. **coolify-manager-rs varsense del GUI — design system (99→36, 0 errores, hecho 2026-08-31, §I-13):** tras el nuevo pedido del usuario («resuelve los problemas de bajo riesgo que quedan (1815)») y con la cache del agregado stale (líneas viejas) se midió baseline fresco por CLI. Borradas **21 clases sin uso** (verificadas 0 referencias .tsx, incl. media query muerto L666-677) y **49 `valorHardcoded` → tokens**: paleta VPS movida a `html:has(.vpsPortal)` + tokens nuevos con consumidor real (`--vpsFuenteBase/Mediana/Titulo`, `--vpsColorFondo78/42`) y fuentes/radios a tokens del design system (`--radioPill`, `--radioCheck`, `--altoTopbar`, `--fuenteXl/Md/Xs`); retirados `--vpsColorBlanco/Negro` y `--fuenteLg` (sin consumidor, grepeado). `token-unused` 6→0; `claseHuerfana` 37→16 (21 reales borradas; 16 restantes = FPs dinámicos §I-2); `token-duplicate` 6→8 solo por límite del detector (no resuelve `var()`: escalas 4px distintas, excepción consistente). 11 one-off sin par de token documentados sin forzar. Verificado `cargo check --tests` exit 0 + `tsc --noEmit` exit 0 + varsense fresco **36 = 0e/25w/1i/10h** + `sentinel analyze` **67 sin regresión** (error = monolito documentado). Commit `308A-6COOL` = `1b48d68`; coolify total **103** (desde 166, −63). Quedan del 1815: las **8 vulnerabilidades dev** (gloryapi esbuild/drizzle-kit, RESTAURANTE concurrently/shell-quote critical) como frente aparte candidato. **Vulnerabilidades dev — resueltas (8→0, 2026-08-31, §I-14):** RESTAURANTE `concurrently ^9.0.0`→`^10.0.5` (shell-quote critical; el fix de npm era downgrade inservible porque 9.2.1 fija 1.8.3 exacto; no se usa en scripts) → audit 0, commit `155a234`. gloryapi override de esbuild a 0.25.12 en el subárbol `@esbuild-kit` de drizzle-kit (verificado 0 referencias en runtime = herencia muerta, usa tsx) + lock regenerado (churn 185 = bumps caret, ningún script roto; gotcha npm 11 con lock existente reproducido en C:/tmp/repro-override) → audit 0. Batería completa verde: tsc + builds server/client (base-ui 1.7) + `npm test` + smoke funcional de drizzle-kit (config vía tsx + migración generada) → commit `7c58531`. Total vulnerabilidades del agregado: **8 → 0**. **Errores de varsense de workspace-manager — resueltos (18→0, 2026-08-31, §I-15):** con `paneles.css` ya en alcance (el usuario confirmó que no hay diff ajeno). Baseline fresco 18e/133w/4i: **15 `--v2-oscuro` + 1 `--alto-consola`** en paneles.css (el doc previo decía 16 `--v2-oscuro`; había 1 runtime extra del mismo bloque) y 2 runtime en v2.css. `--v2-oscuro` era variable inexistente (comentario del switch lo confirmaba) → **15 reemplazos a `--v2-texto`** (token de texto monocromo del design system, 19+ usos en v2). Los **3 runtime** (`--ancho-detalle` 300px/`--ancho-lista` 260px/`--alto-consola` 200px) → defaults declarados en `variables-v2.css` (patrón GUI coolify §I-6: el inline de AppV2 los sobrescribe; defaults = fallback original 1:1). Verificado varsense **18e→0e** sin regresión (133w/4i intactos) + `pnpm run type-check` exit 0 + `sentinel analyze` **60 = baseline exacto** sin hallazgos nuevos. Commit `df737fb`. **workspace-manager queda con 0 errores de varsense y 0 de sentinel** (215 → 197 hallazgos). **Analizador J-8 — FP `claseHuerfana` corregido en varsense (2026-08-31, §J-8):** autorizado por el usuario (decisiones J-1). Commit en el checkout compartido `.quality-tools/varsense` `303e7f9`. (1) **Bug real preexistente `removeComments`**: `current === '\\n'` (doble backslash, string de 2 chars) nunca matcheaba newline → los comentarios `//` nunca terminaban → todo el contenido posterior al primer `//` de cada archivo se destruía → cientos de FPs `claseHuerfana` (caso `mapaV2Cuadricula` en MapaV2.tsx, uso L208 tras comentario L139). Corregido a `'\n'`. (2) `className={expr}` JSX ternarios/identificadores, (3) `createElement('tag','clase')` posicional (Glory-Laminal), (4) `classList.toggle/remove/add`, (5) indirección por variable, (6) MAX_TOKENS 10000→50000. Verificado `check:core` + lint + verificación funcional de los 4 patrones. **Reducciones medidas (0 errores en todos):** Glory-Laminal 91→42 (−49), workspace-manager 137→92 (−45), WANDORIUS 173→157 (−16), coolify 36→32 (−4), RESTAURANTE 140→137 (−3), gloryapi 77→76 (−1); Δ ≈ −118. Efecto colateral: destapó 2 errores reales en gloryapi (`--sortable-transform`/`--sortable-transition` dnd-kit) → defaults declarados en `client/src/index.css` (visual-neutral) → vuelve a 0 errores. **Publicación alineada:** pins `88f281f`→`303e7f9` en 9 consumidores (todos salvo PROYECTO TASKS excluido), locks regenerados, `quality:sync` 10/11 alineados (único desync = PT esperado; WANDORIUS lock legacy stale preexistente documentado). Commits por consumidor `308A-6J8`; docs en plan §J-8 + este log. **CIERRE J-8 — agregado vivo (2026-08-31):** re-análisis forzado por proyecto + `/api/gate/analisis?analizar=todo`: **suma de hallazgos 1716 → 1580 (−136)**. Desglose: Glory-Laminal 42, ws-manager 152 (60s+92v), gloryapi 76, WANDORIUS 157, coolify 99 (67s+32v), RESTAURANTE 247, ONG AGAPE 305 (164s+141v), PT 500 (cap, runtime real bajó pero sigue >500), GLORYPORT 2, freebuff-bridge/GLORYINSPECTOR 0. **Visible en consola ≈ 1591** (1580 + 10 sinPush del frente + 1 gate `varsense ausente` de GLORYPORT; sinCommit/config/vuln en 0). Desde el 1815 del usuario: **−224**; desde el 2571 inicial: **−980**. Nota GLORYPORT (2 vs 1, sin regresión): el server corre sentinel 0.7.4 (runtime RAIZ_VERSIONS) vs fijado 0.7.5 — discrepancia preexistente §I-12; la cache se invalidó por el commit y afloró `directorio-abarotado` de la raíz (11 archivos de manifests, conteo no cambió con J-8); el CLI fijado reporta solo popup.rs (excepción clase §H-1, no forzada). **J-9 (2026-08-31):** cerrado — el fantasma `directorio-abarrotado` de GLORYPORT era **cache stale persistida** del server 8787 (entrada escrita con HEAD viejo `ade2c053` ≠ actual `d2160c0`; el GET /api/gate/analisis sirve la cache sin re-ejecutar). Hipótesis de desalineación de runtime 0.7.4 vs 0.7.5 refutada con corridas directas (conteos idénticos en GLORYPORT/gloryapi/ws-manager). Corrección sin cambio de código: `POST /api/gate/analizar {clave:GLORYPORT, forzar:true}` → GLORYPORT = 1w (`limite-lineas` popup.rs documentado). Decisión: runtime del server NO se cambia (0.7.4 ≡ 0.7.5 en conteos; alineación real = actualizar RAIZ_VERSIONS, decisión de infra del otro hilo). Lección: ante conteo del agregado que no cuadre con el CLI, forzar re-análisis ANTES de sospechar versiones; la cache persistida sobrevive al restart. Residual documentado: la entrada stale de GLORYPORT en data/cache/analisis.json se corrige sola en el próximo sweep auto (el resultado por proyecto y en memoria ya son correctos). Evidencia en plan §J-9. **§J-2 workspace-manager — HECHO (2026-08-31):** sentinel **60 → 29** (0 errores; −31) y varsense 98 intacto (0 errores) → total **127**, dentro del objetivo 110-130 del plan. Resueltos: `console-production`×2 + `fallo-sin-feedback`×2 (logger §H) + `button-clase-especifica`×2 (variante `grande` de Button), `usestate-excesivo`×1 + `componente-sin-hook`×5 (hooks `useMapaV2`/`useLayoutV2`/`usePanelNavegador`/`usePanelDocs`/`usePanelDetalle`, los 3 de panel movidos a `src/hooks/`), `css-adhoc-button-style`×2 + `css-elemento-html-directo`×1 (bloques de botón a `Button.css` exento como compuestos `.botonV2.*`, reset `:is()` misma especificidad), `html-nativo`×11 (usos a `<Button className>` con compuestos), `css-especificacion`×4 (renombres a nombres neutros: `agentsListado`/`resumenTarjeta`/`panelDetalleFilas`/`panelProyectos`, 0 huérfanas nuevas). Excepciones documentadas (29 sentinel + 98 varsense): monolitos limite-lineas ×6, tabs/select sin componente, PanelConfig/PanelConsola hooks forzados, boundary window/dom ×7 + inline dinámico ×3, ISP ×4, directorio-abarrotado ×2, claseHuerfana 45 (FP §J-8), valorHardcoded 42 (frente J-3), token-duplicate 7 (pares paralelos v1/v2 + coincidencias semánticas, patrón §I-9/§I-10), cssInlineReact 4 info. Verificado `pnpm run type-check` exit 0 + `sentinel analyze` 29 (0e) + varsense 0 errores, sin regresión. Commit `308A-6J2`. Evidencia en plan §J-2. **§J-3 ONG AGAPE — HECHO (2026-08-31):** baseline real medido con CLIs fijados: sentinel **164** + varsense **141** = **305** (0 errores; el 324 del encabezado era pre-J-8). Resultado: sentinel **164→110**, varsense **141→131** → total **241** (−64), 0 errores, dentro del objetivo ~150-190 del plan. Resueltos: `emoji-en-codigo` ×8 → entidades HTML (render idéntico), `key-index-lista` ×2 → claves estables, `css-elemento-html-directo` ×25 → `:is()` (misma especificidad), `css-especificacion` ×3 + `button-clase-especifica` ×13 → 51 renames neutros (verificado repo-wide), `css-adhoc` ×4 → Button.css exento, `valorHardcoded` ×10 → tokens con match exacto (`10px` → `--radioTarjeta` ×9, `#fff` → `--colorBlanco` ×1). Excepciones (241, 0e): sqlx `55+6` (deuda §D), hooks/html/usestate en monolitos ×34, limite-lineas ×9, modals ×3, galería de 3 ranuras fijas (key-index ×1, la ranura ES la identidad), valorHardcoded ×72 sin token exacto (one-off), claseHuerfana ×50 (archivado legacy + FP §J-8), token-duplicate/unused ×8 + cssInlineReact ×1. Verificado `npx tsc --noEmit` frontend-v2 exit 0 + type-check frontend exit 0 + varsense sin huérfanas nuevas (claseHuerfana estable 50). Commit `308A-6J3` en ONG AGAPE (26 archivos). Evidencia en plan §J-3. **§J-5 coolify-manager-rs — HECHO (2026-09-01):** baseline fresco con CLIs fijados: sentinel **67** (1e/43w/23h) + varsense **32** = **99**. Resultado: sentinel **67→66** (1e/42w/23h), varsense **32 intacto** (0e), → **98**, sin regresión ni huérfanas nuevas (claseHuerfana 12 estable). **Split real de `limite-lineas`: `src/infra/google_drive.rs` (834 líneas) → módulo `src/infra/google_drive/`** con seam triple de dominio: `mod.rs` (struct + `new()` + helpers + tests), `auth.rs` (`DriveAuthMethod`/SA creds/JWT/tokens, `pub(super)` para visibilidad entre hermanos), `files.rs` (operaciones Drive upload/download/list/delete/ensure/find/metadata). El hallazgo `limite-lineas` de google_drive desapareció del analyze; cero cambio de comportamiento (misma lógica, `cargo test --lib` 172/172). Excepciones documentadas sin forzar: `mcp/tools.rs` (872 efectivas, seam único defs/ejecución deja ~507 residuales → split no alcanzaría límite 500, excepción de flujo), `portal.css` (904/600 GUI §I-6) y `deploy_service.rs` (2135, monolito §H-1 con su nivel-3 = único error). 36+22 monolitos, 16 claseHuerfana FP, 11 valorHardcoded one-off, 8 token-duplicate escalas = excepciones §I-13 intactas. Verificado `cargo check --tests` exit 0 + `cargo test --lib` 172/172 + `sentinel analyze` 66 (0e nuevos) + `varsense all` 32 (0 errores). Commits: `308A-6J5` (coolify-manager-rs) + docs workspace-manager. **CIERRE FINAL DEL CICLO 308A-6 (2026-09-01, §J-7 HECHO):** GET `/api/gate/analisis?analizar=todo` tras sweep forzado de entradas stale (RESTAURANTE/coolify re-analizados post-J): **TOTAL visible 1473** (cap 500/proyecto). Desglose 1:1 con baselines post-J: PT 500 (cap, WIP usuario, excluido), ONG AGAPE 241, RESTAURANTE 231, WANDORIUS 157, workspace-manager 127, coolify 98 (1e monolito), gloryapi 76, Glory-Laminal 42, GLORYPORT 1, freebuff-bridge/GLORYINSPECTOR 0. Excluyendo PT: **973** (desde 1079 §J-8/9 y 1580 cierre J-8; desde el 2571 inicial −1598, desde el 1815 −842). Errores totales: **1** (deploy_service §H-1), sinCommit 0. Todos los frentes J cerrados (J-2…J-6, J-8, J-9; no existe J-10); nada abierto fuera de excepciones documentadas. **Bloque 308A-6 cerrado.** **308A-7 NO declarado (verificado 2026-09-01):** roadmap/AGENTS.md contienen 0 referencias a 308A-7 — el nombre solo existía como card de follow-up del agente. Próximo ciclo = decisión del usuario sobre los candidatos del plan §J-10 (RESTAURANTE sqlx `query_as!`, runtime del server 8787 → 0.7.5, PT tras aterrizar su WIP, sweep post-PT). **308A-7V2 — conteo consola vs CLI alineado + FPs `claseHuerfana` (2026-09-01, §J-11):** dos defectos reales corregidos. (1) **Server 8787:** `normalizar()` truncaba hallazgos a 500 → los conteos por regla del agregado no coincidían con el CLI (PT claseHuerfana 298 en consola vs 1083 reales); fix: hallazgos COMPLETOS + `hashArtefacto` (size+mtime de out/cli y dist/cli) en la clave de frescura para que cualquier rebuild invalide la cache. (2) **Analizador varsense `8ce45b6` (tag `v2.2.1-j8b`):** el scanner no veía props transportadoras de clase del design system (`claseExtra`/`claseAdicional`/`*clase`) ni templates de declaración >240 chars → `prop-clase` generalizada + MAX_TOKENS 10000→50000; tests 64/64 + lint clean; **0 FN verificado** (las 319 clases que dejaron de reportarse en PT tienen uso repo-wide real). **Resultado agregado vivo: 2509 → 2203** (PT 1230 con claseHuerfana 777, 0 errores; 1 error area-wide = monolito deploy_service documentado; resto de proyectos sin regresión, consola y CLI muestran el mismo número por regla/proyecto). Release J-8-style: pins `303e7f9`→`8ce45b6` + locks regenerados en 9 consumidores (workspace-manager `d43456d` incluye el fix del server; coolify `4dbf793`, ONG AGAPE `97bb1dd`, RESTAURANTE `bd8f34b`, Glory-Laminal `ea5ad3e`, GLORYPORT `4e758da`, freebuff-bridge `e9f5e49`, GLORYINSPECTOR `176d79e`, PROYECTO TASKS `f09497a` solo qt.json+lock con tu WIP intacto; WANDORIUS `6027bee4`+`1e8722b4`; gloryapi no pinnea varsense — gate legacy, sin cambio). Scratch de C:/tmp limpio. **308A-7V3 — bug de comillas del scanner `claseHuerfana` cerrado (2026-09-01, §J-11V3):** usuario reportó que ~35 de las 464 restantes de PT «tienen uso real» y pedía arreglar la detección. Causa raíz: `REGEX_STRING_LITERAL` falla con literales vacíos (`x ? 'a' : ''`) → traga la siguiente pareja de comillas (pierde `panelCelda--origenMover`) y emite un identificador basura como clase. Fix en el analizador (`50913d2`, tag `v2.2.1-j8c`): extractor de literales por escaneo de caracteres + `**/*.css` como consumer pattern + props `clase:`/`switch/case` como usos; tests 67/67 + lint + check:core. **PT: claseHuerfana 464→429 (−35), 0 errores, 0 FN verificado; sin regresión en otras reglas** (el +1 valorHardcoded es regla del WIP del usuario, documentado). Release J-8-style: pins `8ce45b6`→`50913d2` + locks en 10 consumidores (workspace-manager `7e05d14`, coolify `999b50a`, ONG AGAPE `ce373f1`, RESTAURANTE `6971b18`, Glory-Laminal `fb8f08b`, GLORYPORT `ef9e40c`, freebuff-bridge `5e264b6`, GLORYINSPECTOR `0413bda`, PT `8fd6209` solo qt+lock con tu WIP intacto, WANDORIUS `d80b83ee`+`734595b8`); dist del submodulo de WANDORIUS reconstruido desde el pin (el CLI viejo no tenía el fix). Sin push. Scratch limpio. Los 429 restantes: 363 con uso real invisible al scan estático (siguiente iteración del indexador), 76 muertas en reglas mixtas conservadas por diseño, coincidencias de datos. **308A-7V4 — consistencia CLI↔provider de `sentinel-disable` + `cssInlineReact` de PT (2026-09-01, §J-11V4):** bloque declarado en roadmap como siguiente accionable (PT cssInlineReact). **Causa raíz de la inconsistencia de conteo:** **77 de 112** `cssInlineReact` de PT ya llevan `sentinel-disable inline-style-prohibido` en el código (excepción declarada para sentinel con justificación), pero el core del CLI de varsense los ignoraba — el provider de VS Code sí los honra → misma línea reportada al CLI pero no en el editor. Fix en el analizador (`beb05ba`, tag `v2.2.1-j8d`): el core ahora honra `sentinel-disable`/`varsense-disable` (misma lógica del provider, por línea y por bloque); tests **71/71** + lint + check:core. **Fix real adicional:** `iconStyle = {width:18,height:18}` estático de PageLayout.tsx (7 svgs) → clases Tailwind `w-[18px] h-[18px]` (el archivo ya usa arbitrary values: `max-w-[1200px]`, `bg-[#1a1a1a]`; visual idéntico). **Resultado PT: cssInlineReact 112→29**, total varsense **821→742, 0 errores** (el 1 `variableNoDefinida` es regla del WIP del usuario en panelIA.css, documentado), claseHuerfana 429 estable, **0 FN auditado** (los 77 suprimidos sientan sobre/después del comentario disable; los 6 iconStyle arreglados de verdad). Los **29 restantes = estilos dinámicos runtime** (`estiloGrid`/`estiloArea`/posiciones/progresos %) documentados como excepción (no pueden ir a CSS estático). Verificado: `npm run type-check` exit 0 + build de PT verde + `sentinel analyze` PT **96 (0e/88w/8h) = baseline exacto** sin hallazgos nuevos. Release: pins `50913d2`→`beb05ba` + locks regenerados en 10 consumidores (workspace-manager `31aa0f2`; ONG AGAPE pin corregido tras mangle del hash; PT `91e3ee0` incluye qt.json+lock+PageLayout.tsx con tu WIP intacto — trees verificados; WANDORIUS gitlink+lock+dist del submódulo reconstruido). **Verificación viva:** re-análisis forzado `POST /api/gate/analizar` → el server 8787 reporta **PT varsense 742 con cssInlineReact 29** — el runtime ejecuta el fix V4, no solo el CLI local. Sin push (decisión del usuario). Scratch de C:/tmp limpio. Evidencia en plan §J-11V4. **308A-7V5 — `valorHardcoded` de PT cerrado (2026-09-01, §J-11V5):** bloque declarado como siguiente accionable (43 `valorHardcoded`, 0 errores). Baseline varsense PT `739 = 1e/738w` (único error = `variableNoDefinida` del WIP del usuario en panelIA.css, documentado). Los 43 son CSS puro en 5 archivos (App.css ~20, constructorPaginas.css 11, glory-core/index.css 7, recordatorios.css 3, layoutManager.css 2). **3 reales con match exacto de token:** `background: #0f172a` ×3 en App.css ≡ `--item-surface` del propio `:root` → `var(--item-surface)` (visual idéntico). **40 = excepción fundamentada:** font-size px one-off del constructor (la escala de tokens lleva `ajusteTipografia` −1.5px, sin pares exactos), radios del canvas (tema monocromo todo 0), valores de runtime desktop/glory-core y de recordatorios/layoutManager sin par en los tokens del dashboard — no se fuerza token sin segundo consumidor real. **Resultado PT: `valorHardcoded` 43→40** (−3 exactos), total **739→736**, 0 errores nuevos, claseHuerfana 429 estable (sin huérfanas nuevas). Verificado: `npm run type-check` exit 0 + `vite build` verde + `sentinel analyze` (0.7.7/0559576) **95 (0e/87w/8h) = baseline exacto** sin hallazgos nuevos. Commit local **PT `3368dc6`** (`318A-7V5`), stage explícito solo de `frontend/src/App.css`; WIP del usuario intacto. Sin push (decisión del usuario). Evidencia en plan §J-11V5.§J-11V5. **308A-7V6 — cssInlineScript de PT cerrado (2026-09-01, §J-11V6):** bloque declarado como siguiente accionable (32 `cssInlineScript` de PT, varsense 736 = 1e/735w, runtime fijado `beb05ba`). Clasificación 32/32: TODOS son mutaciones runtime imperativas (`document.body.style.cursor`/`userSelect`/`overflow` durante drag/resize/modal y `el.style.left/top` con coordenadas getBoundingClientRect) sin seam estático — excepción documentada, CERO código tocado, sin disables nuevos. Los locks de body.overflow (useModal/useModoEnfoque) tienen seam teórico a clase `body.modalAbierto` pero tocan 11 hooks de UX crítica sin prueba real de interacción; mejora futura enmarcada, no forzada. Verificado: type-check frontend exit 0, sentinel PT 95 (0e/87w/8h) = baseline exacto sin hallazgos nuevos, varsense 736 estable con `cssInlineScript` 32, WIP del usuario intacto. Sin commit en PT (nada que commitear); docs en plan §J-11V6. PT sigue ahead 9 de origin; push pendiente de decisión del usuario.

Detalle en `PLAN-saneamiento-agents-y-analisis.md`, `PLAN-corregir-1408.md` y registro en `data/inventarios/s2-plan-reparaciones-20260830-012405.md`.

## Bloqueos y decisiones

- Ningún backup se considera válido sin manifest y hash coincidente.
- No se borra contenido si no existe rollback verificable.
- Worktrees, referencias, configuraciones, locks y `AGENTS.md` se procesan individualmente y con evidencia concreta.
- Las carpetas normales no se clasifican como árboles huérfanos.

## Tareas pendientes (orden de dependencia)

1. ~~`308A-2 / S2-05` — resolver VarSense por fuente oficial o documentar limitación.~~ **RESUELTO (stale):** VarSense ya está provisionado y compilado en el checkout compartido (`88f281f` v2.2.1, `tools/varsense` de RESTAURANTE); el bloqueo era un supuesto stale (ver 308A-1 F1 y PLAN-centralizar-gate.md).
2. ~~`308A-2 / S2-12` — refactors de código: Glory-Laminal 12→0 (eebe026); **gloryapi COMPLETO 8→0** (F5, `4e97e2a`, 315/315 tests OK); RESTAURANTE piso honesto **120** pusheado; **PROYECTO TASKS 500→23** (console-production 86→0 con logger central `ac5d4c4`; restante = excepciones legítimas: emoji 9, inline-style 5, monolitos de API 9); **WANDORIUS COMPLETO 410→0** (`c1af8af6`, analizador 0.7.4 = 0/481; único matiz: primaria `wandorius` vs rama real `main`).~~ **RESUELTO — los 5 frentes a su piso honesto con evidencia** en `PLAN-corregir-1408.md` §7.
3. ~~`308A-2 / S2-08` — revisión SOLID, eficiencia y regresiones de la detección nueva.~~ **COMPLETO** — auditoría del frente completo (analizador/proveedor/endpoints/timer/consola): corregido el único defecto real (caché sin evicción de claves obsoletas; `analizarTodo` poda por snapshot, verificado con área aislada). Sin otras regresiones; detalle en `PLAN-analisis-sentinel-consola.md` §9.
4. `308A-1` — **centralizar runtime del gate** (checkout compartido `.quality-tools/` + `sourcePathEnv` + `quality:sync`); `glory-sentinel` queda exento de gate (excepción en el panel). **HECHO (2026-08-30):** F0/F1 ✅ checkout compartido (`sentinel@643353d` v0.7.5 + `varsense@88f281f` en `area-trabajo/.quality-tools/`, **compilados/provisionados**, detached/limpios, ignorado por el scanner — S2-05 era stale y quedó resuelto); F5 ✅ `quality-sync.mjs`/`sync:quality` (commit `64c4ee5`, validación fail-closed del commit común; exit 1 DESYNC 2: WANDORIUS/RESTAURANTE 0.7.4 vs compartido 0.7.5, el paso pendiente de F2); F6 ✅ excepción `sinGate` en workspace-manager (v3 + endpoint `/api/config/singate` + subsección «sin gate»; `glory-sentinel` visible sin problema). Derivación de env en el server hecha (commit `2ae5f10`: `entornoGate()` en `analizador.ts` deriva `GLORY_*` al invocar sentinel, sin pisar overrides). **F3 ✅ / F2 parcial:** mecanismo final = `sourcePath`+`provisionPath` relativos a `../.quality-tools/` (sin OS env, portable — «Opción A gestionada»). Migrados y verificados: **gloryapi** `e0981cd` (doctor readyForGate:true con env vacía), **PROYECTO TASKS** `ef65283` (sentinel+varsense compartido), **RESTAURANTE** `2f53e39a` (full hash `643353d7e968…`, doctor del frame verde). `quality:sync` pasa a **`problemas: 1`** (solo WANDORIUS). **F2 WANDORIUS ⏸ BLOQUEADO con evidencia**: su frame bespoke no usa `provisionPath`, lleva install/lock propios y al repuntar al compartido el `doctor` exige `quality:setup` (rebuild + locks) — revertido a su 0.7.4 verificado; requiere o un `quality:setup` dirigido (decisión) o armonizar su frame (más alcance). **F4 Glory-Laminal/ONG AGAPE ✅ REGISTRO (2026-08-30)**: `quality-tools.json` (sentinel+varsense `../.quality-tools/`, full hash) + `varsense.config.json` creados; `quality:sync` los da **✓ (alineados)** — `problemas: 1` (solo WANDORIUS). **F4 BOOTSTRAP ✅ (2026-08-31)**: cerrado el sub-paso — portada la maquinaria mínima de provisión (setup con evidencia release real + lock con commit/sha256) a ambos repos y el `doctor` del shim queda **`readyForGate:true`/issues `[]`**: Glory-Laminal `edbd1e7`, ONG AGAPE `896a864` (este último corrige las rutas a `../../.quality-tools/` por su nivel extra en TRABAJOS CLIENTES/ y re-incluye `scripts/quality/` en la whitelist del `.gitignore`). `quality:sync` alineado ✓ en ambos (ver `PLAN-centralizar-gate.md`). **F7 ✅ HECHO (2026-08-31, commit `973158c`, publicado):** vista «gate centralizado» en el PanelConfig (módulo `src/server/gate/sincronizacion.ts` + endpoint `GET /api/gate/sincronizacion` + store `cargarSincronizacion`) que reusa `quality-sync.mjs --json` vía spawn (sin duplicar validación) y muestra badge por consumidor `✓ alineado`/`desync` con detalle por herramienta; verificado en vivo: Glory-Laminal/gloryapi/PT/RESTAURANTE/ONG AGAPE `ok`, WANDORIUS `desync` (`0349485…` ≠ compartido `643353d…`), `problemas: 1`. **F2 WANDORIUS ✅ HECHO (2026-08-31, commit `60879ac3`):** el manifest pasó de submódulos locales `tools/{sentinel,varsense}` (0.7.4) a `sourcePathEnv` (`GLORY_SENTINEL_SOURCE_PATH`/`GLORY_VARSENSE_SOURCE_PATH`), que el server deriva al checkout compartido — el modo externo que el frame bespoke soporta (su chequeo de integridad de gitlink rechaza un `sourcePath` relativo `../`; por eso `quality:setup` no aplica aquí, igual que en RESTAURANTE). Commits compartidos: sentinel `643353d` v0.7.5, varsense `88f281f` v2.2.1. `quality-sync` y el endpoint F7 confirman **WANDORIUS alineado — `problemas: 0`** (los 6 consumidores ✓). Con esto **308A-1 queda COMPLETO: F0/F1/F3/F4-bootstrap/F5/F6/F7 ✅ y F2 ✅**. **EXTENSIÓN 308A-1 (2026-08-31, decisión del usuario «todos deben tener Sentinel excepto glory-sentinel»):** bootstrap de gate a los 5 repos que no lo declaraban — `coolify-manager-rs` `402a427`, `freebuff-bridge` `1d5b8da`, `GLORYINSPECTOR` `51f3e7b`, `GLORYPORT` `ade2c05` y `workspace-manager` `fa69380`. Cada uno con `quality-tools.json` + `sentinel.config.json` (preset por stack: mixed/node/python/rust/node) + `varsense.config.json` (patterns por stack) + `scripts/quality/` (setup+lock, idénticos al patrón validado en Glory-Laminal/ONG AGAPE) + `sentinel.lock.json` generado (commit compartido `643353d`/`88f281f` + sha256) + `.gitignore` con `.sentinel/` (y en coolify-manager-rs negaciones `!quality-tools.json`/`!sentinel.config.json`/`!sentinel.lock.json`/`!varsense.config.json` porque su `.gitignore` tiene `*.json`). `quality-sync.mjs` ampliado a **11 consumidores**; doctor en los 5: **`readyForGate:true`/issues `[]`**; `quality-sync` → **`problemas: 0`, OK: 11 consumidores alineados**. Commits locales, sin push.
4. `308A-3` — **corregir los hallazgos de Sentinel en los proyectos que hoy no tienen gate** ~~(depende de 308A-1)~~. **COMPLETO (piso honesto, 2026-08-31):** aplicado el analizador 0.7.4 a los 3 proyectos relevantes (gloryapi y Glory-Laminal ya cerraban en 0) con la disciplina del hilo 1408: refactors reales verificados con `cargo check --tests`/`tsc --noEmit`, excepciones legítimas documentadas, **sin disables para bajar conteo** (solo mecanismos canónicos: boundaries, exclusiones, logger central). Conteos finales por proyecto (ver `PLAN-corregir-hallazgos-post-gate.md`): **GLORYPORT 14→1** (`85a022b`, unwraps de Mutex con recuperación de poison; queda `popup.rs` monolito documentado), **workspace-manager 100→86** (`80db0db`, imports muertos, spawn sin shell, catch, barras-decorativas; resto = excepciones de UI/console del propio front), **coolify-manager-rs 145→123** (`5ddc908`+`b01f0e0`, `stack_uuid` unwraps → validación canónica; quedan funcion-larga/parametros/CSS/window como excepciones de producción documentadas). No se forzaron refactors de gran superficie de API/UI en la herramienta de producción.
5. `308A-4` — **detectar vulnerabilidades de dependencias en la consola** (nueva categoría): ejecutar la auditoría local por proyecto (pnpm/npm/cargo audit según lockfile), **automáticamente** vía el timer `scan` existente, agrupada por proyecto con badges de severidad y **conteo separado** del resto de la consola, con caché por hash de lockfile y single-flight. Reutiliza el infraestructura de análisis ya commiteada (A0–A4+R). Origen: GitHub solo notificó a `workspace-manager` (7, 2 high/5 moderate) al pushear; la auditoría local ya mide **workspace-manager 4** (vite) y **gloryapi 23** (2 low/7 moderate/11 high/3 critical), con Glory-Laminal y freebuff-bridge en 0. **V1 HECHO / V2 HECHO (2026-08-30):** detector `src/server/gate/vulnerabilidades.ts` (cola serial + single-flight + caché por hash-de-lockfile + timeout 120 s), endpoints `/api/gate/vulnerabilidades` (POST single) y `-todo`/`-cache` (GET rehidratar), categoría `vulnerabilidades` en la consola (badges por severidad, conteo separado del resto), botón «auditá toda la consola» en el PanelConfig, y auto-timer (el único `temporizadorAuto` dispara `escanearTodo`+`auditarTodo`, cada uno con single-flight propio y `.catch`, cero recursos con la app cerrada). Verificado en vivo: workspace-manager 4, gloryapi 23, RESTAURANTE 2 critical, glory-sentinel 2, Glory-Laminal 0. **V3 HECHO (2026-08-31):** `cargo-audit` instalado global (vía `cargo install cargo-audit`, build en `C:/tmp`, limpiado después; `cargo audit --version` OK, nada del entorno roto). El detector `vulnerabilidades.ts` ya invocaba `cargo audit --json` automáticamente, así que los repos Rust dejaron de ser no-auditables sin cambio de código: **GLORYPORT cargo audit = 0 hallazgos** (1226 advisory database, 24 deps). Cobertura: GLORYPORT, WANDORIUS, PROYECTO TASKS, coolify-manager-rs. **R HECHO (2026-08-31):** revisión SOLID/eficiencia del frente — corregido el único defecto real (botón «auditá ahora» por proyecto en PanelDetalle que el plan V1 exigía y nunca se cableó; `auditarUno`/endpoint quedaban como código muerto), verificado type-check exit 0 y corrida real `POST /api/gate/vulnerabilidades` (workspace-manager: 1 high + 3 moderate). **DEPENDENCIAS (2026-08-31):** corregidas con bumps canónicos y stage explícito de `package.json`/lockfile: **workspace-manager** (`b888e57`) vite `^5.4.0→^6.4.3` (resolve esbuild fuera del rango vulnerable) → `pnpm audit` a **0**; **gloryapi** (`fdc767e`) concurrently/drizzle-orm/tsx/vitest/client-vite/react-router-dom + lockfile regenerado → `npm audit` **23→0 en producción** (críticos 3→0, high 11→0); quedan **4 moderate dev-only** en la cadena `drizzle-kit→esbuild-kit` sin fix ascendente, documentados como excepción. **COMPLETO.** Detalle en `PLAN-vulnerabilidades-consola.md` §7.

## Planes activos

- `PLAN-cero-deuda-2026-09-02.md` — campaña a CERO de la deuda real confirmada (F1→F5 + fase final de decisiones); frente de deuda real del área.
- `PLAN-corregir-restantes-sentinel-varsense.md` — 308A-5, corrige los hallazgos restantes de Sentinel/VarSense (en curso; su frente de deuda real pasa a PLAN-cero-deuda).
- `PLAN-saneamiento-agents-y-analisis.md` — S2 en ejecución controlada.
- `PLAN-centralizar-gate.md` — runtime del gate compartido y único; `glory-sentinel` exento. (Sustituye la dirección de `PLAN-agregar-gate-proyectos.md`.)
- `PLAN-gate-dinamico.md` — proveedores dinámicos, R1/E1/E2/E3 completadas.
- `PLAN-analisis-sentinel-consola.md` — análisis de Sentinel en consola, A0–A4 + R completadas.
- `PLAN-reglas-completas-tabs.md` — catálogo completo de reglas con tabs.
- `PLAN-vulnerabilidades-consola.md` — detección automática de vulnerabilidades por proyecto (308A-4).
